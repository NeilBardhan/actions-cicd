name: build-test
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: get-code
              uses: actions/checkout@v4
              with:
                fetch-depth: 2
            - run: |
                git checkout HEAD^
                git status
            - name: build
              run : |
                echo $GITHUB_WORKSPACE
                ls